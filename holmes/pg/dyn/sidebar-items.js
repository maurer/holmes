initSidebarItems({"mod":[["types","This module defines the trait new types must implement, along with several core types to avoid the need to rewrite basic types every time. It is heavily codependent on the `values` module."],["values","This module defines the trait new values must implement, along with several core values to instantiate the basic types provided in `types`. It is heavily codependent on the `values` module."]],"trait":[["HashTO","`HashTO` is a Trait Object-safe version of the Hash trait, using a reference to a `Hasher` rather than a polymorphic one in order to allow construction of trait objects."]],"type":[["Type","Represents the type of a dynamic value as a threadsafe trait object."],["Value","Represents a dynamic value as a threadsafe trait object."]]});